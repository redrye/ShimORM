class ModelEvents {
    BOOTING = "booting"
    BOOTED = "booted"
    CREATING = "creating"
    CREATED = "created"
    UPDATING = "updating"
    UPDATED = "updated"
    DELETING = "deleting"
    DELETED = "deleted"
    FORCE_DELETING = "forceDeleting"
    FORCE_DELETED = "forceDeleted"
    RESTORING = "restoring"
    RESTORED = "restored"
    FETCHING = "fetching"
    FETCHED = "fetched"

}

export default ModelEvents